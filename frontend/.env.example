# Template file: copy to .env and fill in real values.
# Base URL of the deployed backend API (no trailing slash).
# Local dev: http://localhost:4000
# Production: your Render service URL, e.g. https://life-rpg-api.onrender.com
VITE_API_URL=http://localhost:4000
